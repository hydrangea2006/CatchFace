# Python 运行产生的字节码文件（必须忽略，否则会导致 Git 记录混乱）
__pycache__/
*.py[cod]
*$py.class

# 虚拟环境（虽然 VS Code 自动帮你生成了一个，但在根目录再写一遍是双重保险）
.venv/
venv/
ENV/

# 操作系统生成的临时文件
.DS_Store
Thumbs.db

# 编辑器/IDE 配置（防止你的个人窗口布局、路径配置影响队友）
.vscode/
.idea/
*.swp

# 某些 AI 库下载的本地模型权重（如果文件很大，建议忽略并让队友自行下载）
# models/*.pth
# models/*.onnx